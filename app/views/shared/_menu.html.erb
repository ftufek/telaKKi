<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a data-target=".nav-collapse" data-toggle="collapse" class="btn btn-navbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>

      <%= link_to "telaKKi", root_path, class: "brand" %>
      <div class="btn-group pull-right">

        <% if user_signed_in? %>
          <a href="#" data-toggle="dropdown" class="btn dropdown-toggle">
            <%= icon_for("user") %> My Account
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Profile", users_profile_path %></li>
            <li class="divider"></li>
            <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          </ul>
        <% else %>
          <%= link_to new_user_session_path, class: "btn" do %>
            <%= icon_for("user") %>
            <%= "Sign In" %>
          <% end %>
        <% end %>
      </div>

      <div class="nav-collapse">
        <ul class="nav">
          <li class=""><%= link_to "(Be part of the solution!)", solution_path %></li>
          
<!--          <li class="divider-vertical"></li>

          <li class=""><%= link_to "Post an Ad", new_ad_path %></li>

          <li class="dropdown">
            <a data-toggle="dropdown" class="dropdown-toggle" href="#">Show Categories <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <% Category.where(parent_id: nil).each do |t| %>
                <li>
                  <%= link_to t.name do %>
                    <%= icon_for(t.icon_name) %>
                    <%= t.name %>
                  <% end %>
                </li>
              <% end %>
            </ul>
          </li>-->
        </ul>

        <form class="navbar-search" action="">
          <input class="search-query span3" placeholder="Search something..." type="text">
        </form>


      </div>
    </div>
  </div>
</div>
